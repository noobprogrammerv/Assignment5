package validator;
                                                         // la seminar era Exception
public class ValidateException extends RuntimeException {// eu le am bagat la runtime exception
    public ValidateException(String message) { super(message); }
}
